<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AndroidSecureCrypto">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".random.RandomActivity"
            android:label="Random Number Samples"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".cipher.CipherActivity"
            android:label="Cipher Samples"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".keyderivation.KeyDerivationActivity"
            android:exported="false" />

    </application>
</manifest>